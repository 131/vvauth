"use strict";


class vcreds {



}


if(module.parent === null) //ensure module is called directly, i.e. not required
  require('cnyks/lib/bundle')(vcreds); //start runner


module.exports = vcreds;
